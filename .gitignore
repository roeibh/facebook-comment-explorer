node_modules/
*.log
.DS_Store

# Everything below is generated by `npm run build` / `npm run shots` and rebuilt
# on every deploy, so the source files are the only thing tracked.
dist/
docs/index.html
docs/bookmarklet.txt
docs/og.png
docs/screenshot.png
