# facebook-comment-explorer
Bookmarklet that expands every comment and reply on a Facebook post, then lets you search them, filter by thread, and export to CSV/JSON. No extension, runs locally in your browser.
